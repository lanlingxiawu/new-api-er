# 统一受管流式大小上限为 100 MiB

## 后续确认：200 MiB

用户随后明确确认统一上限调整为 **200 MiB（209,715,200 字节）**，本节覆盖下文历史 100 MiB 数值。保留文档文件名以维持既有链接。

实施范围：公共常量调整为 `200 << 20`，Claude 别名及现有受管 JSON/SSE/WS/工具限额同步生效；诊断截取、资金结算、断开、超时、旧扫描器独立配置均不变。先更新测试，再改常量，验证原 100 MiB 以上及 200 MiB 前后边界。

Main Chain Impact / Shared Resource Audit：无新增 DB/Redis、网络调用、协程或共享锁，缓冲按需增长。相对 100 MiB 最坏单缓冲预算翻倍，100 个接近限额的缓冲有效字节约 19.53 GiB；多层复制可能进一步增加内存。100k RPM 容量需按同时存在的大响应数衡量，边界测试按包串行运行，不代替生产压测。

200 MiB 验证记录：常量测试先观察到旧值 104857600 与新要求不符，再修改实现。8 MiB+1、100 MiB+1、200 MiB-1、200 MiB、200 MiB+1 的完整图片 JSON 边界通过；common/helper/Claude/OpenAI/讯飞/火山六个相关包串行全量测试通过，包含诊断截取及工具、媒体回归。未执行生产压测或部署。

2026-09-15 用户明确确认按 100 MiB（104,857,600 字节）实施。

## 范围与数据流
将原受管流式共用的 8 MiB 上限调整为 100 MiB，覆盖单事件、完整 JSON、下游帧/批次、工具参数及受管 WS 消息。Claude 专用流引用同一公共常量，避免限额漂移。原普通扫描器的独立配置、HTTP 请求体限额、裸媒体逐块转发与诊断首尾截取保持原状。

## API、配置、数据模型与错误
不新增 API、表、迁移或配置。沿用现有功能开关、JSON/协议校验、超限错误分类、计费及断开逻辑。恰好上限允许，超过上限拒绝；这是有效字节限制，不是进程内存上限。

## Main Chain Impact / Shared Resource Audit
仅修改既有边界值和 Claude 常量引用，无新增同步/异步调用、DB/Redis 操作、连接、协程或共享锁。请求局部缓冲按需增长，不预分配 100 MiB。诊断仍保留首尾各 1 KiB；工具数量仍限制 128 项。

## Concurrency Analysis
100k RPM 下每请求新增 DB/Redis 调用和协程均为零。最坏缓冲预算增加 12.5 倍，读取、解析、接收/交付工具跟踪及队列可能同时持有多份数据；100 个同时接近限额的缓冲仅有效字节就约 9.77 GiB。必须按并发大响应数量核算容量，单机测试不替代生产负载测试。

## 验证
先新增精确常量及完整图片 JSON 边界测试（原 8 MiB 以上、100 MiB 前后），再调整实现。既有事件载荷与工具预算测试自动跟随公共常量，检查 Claude 共用值及诊断窗口不变。大数据测试按包串行运行，避免测试并发放大峰值内存。

## 实现结果
- 公共常量现为 `100 << 20`，Claude 使用公共常量别名；其他受管扫描、写入、工具及 WS 限制通过既有引用同步生效。
- 常量测试先验证旧值 8388608 与新要求不符，再修改实现；完整图片 JSON 的 8 MiB+1、100 MiB-1、100 MiB、100 MiB+1 四个边界均通过，超过上限仍按 upstream_protocol_error 终止。
- `go test -p 1 ./relay/common ./relay/helper ./relay/channel/claude ./relay/channel/openai ./relay/channel/xunfei ./relay/channel/volcengine -count=1` 全部通过；覆盖既有工具/错误载荷上限及裸媒体逐块读取回归。Claude 共享常量断言单独通过。
- `go build ./...` 与 `git diff --check` 通过。未部署、未执行生产并发大响应压测。
