# 打包部署流程

文档地址：<https://docs.newapi.pro/zh/docs/installation/deployment-methods/local-development#%E5%AE%89%E8%A3%85-bun%E6%8E%A8%E8%8D%90>

## 一、前端打包

```bash
cd web
bun install
bun run build
```

## 二、服务端打包（Linux 部署）

```bash
Linux上$env改用 export

go mod download

$env:CGO_ENABLED="0"
$env:GOOS="linux"
$env:GOARCH="amd64"
go build -ldflags="-s -w" -o new-api main.go
```

## 三、不打包前端的后端构建

```bash
$env:CGO_ENABLED=0
$env:GOOS=linux
$env:GOARCH=amd64
go build -ldflags="-s -w" -o new-api backend_only_main.go
```

## 四、服务器部署

### 1. 上传文件

将 `new-api` 上传到服务器，并赋予执行权限：

```bash
chmod +x new-api
```

### 2. 配置环境变量

编辑 `.env` 文件，并上传到程序同级目录。
如需参考示例，可复制 `.env.配置示例`或 `.env.example` 后再修改。

### 3. 开启错误日志

```bash
export ERROR_LOG_ENABLED=true
```

### 4. 启动服务

```bash
./new-api
```

### 5. 后台启动

```bash
export ERROR_LOG_ENABLED=true
nohup ./new-api > output.log 2>&1 &
```

