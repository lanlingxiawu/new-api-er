# 自动分组/模型状态监控与提醒系统 — 功能设计方案

**版本**: 1.0  
**日期**: 2026-06-02  
**作者**: AI Design Team  
**状态**: 设计阶段

---

## 1. 概述

### 1.1 目标

设计一套完整的**分组/模型健康状态监控系统**，实时跟踪分组和模型的可用性、性能、配额等状态，并在仪表盘、用户令牌(API Keys)、模型广场(Pricing)等位置展示状态信息，主动向用户提醒状态变化和优化建议。

### 1.2 现状分析

**已有功能：**
- ✅ 自动测试通道机制（每N分钟测试一次）
- ✅ 分组和模型的关联管理
- ✅ 渠道自动禁用启用机制
- ✅ 分组倍率配置系统
- ✅ 定时任务框架（Goroutine + Ticker）
- ✅ 消费日志记录

**缺失功能：**
- ❌ 分组/模型的专用状态表（仅记录消费日志）
- ❌ 状态变化历史追踪
- ❌ 性能趋势分析
- ❌ 用户友好的状态展示UI
- ❌ 智能提醒/建议机制
- ❌ 自动分组切换推荐

### 1.3 核心改进点

| 维度 | 现状 | 改进后 |
|------|------|--------|
| **数据存储** | 仅消费日志 | 专用状态表 + 历史记录 |
| **可见性** | 仅管理员可查看测试结果 | 用户也能看到自己的分组/模型状态 |
| **提醒机制** | 无 | 基于规则的自动提醒系统 |
| **决策支持** | 无 | 性能对比、推荐最优分组/模型 |
| **时间维度** | 单点测试 | 历史趋势、周期分析 |

---

## 2. 详细功能设计

### 2.1 数据模型设计

#### 2.1.1 新增表：`group_status` — 分组状态表

```sql
CREATE TABLE group_status (
  -- 基础信息
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_group VARCHAR(64) NOT NULL COMMENT '用户分组标识',
  group_name VARCHAR(128) COMMENT '分组显示名称',
  
  -- 状态指标（实时）
  status TINYINT DEFAULT 1 COMMENT '状态: 1=健康, 2=警告, 3=不健康, 4=不可用',
  last_test_time BIGINT COMMENT '最后一次测试的Unix时间戳(秒)',
  success_rate DECIMAL(5,2) DEFAULT 100 COMMENT '最近24h成功率(%)',
  avg_response_time INT COMMENT '最近24h平均响应时间(ms)',
  
  -- 关键指标
  total_channels INT DEFAULT 0 COMMENT '该分组下的渠道总数',
  available_channels INT DEFAULT 0 COMMENT '可用渠道数',
  disabled_channels INT DEFAULT 0 COMMENT '被禁用的渠道数',
  
  -- 性能指标
  p50_response_time INT COMMENT '中位数响应时间(ms)',
  p95_response_time INT COMMENT '95分位数响应时间(ms)',
  error_count INT DEFAULT 0 COMMENT '最近24h错误次数',
  
  -- 分组配置快照
  quota_multiplier DECIMAL(8,2) DEFAULT 1.0 COMMENT '该分组的倍率',
  is_auto_group TINYINT DEFAULT 0 COMMENT '是否为自动分组',
  
  -- 时间戳
  created_at BIGINT NOT NULL,
  updated_at BIGINT NOT NULL,
  
  -- 索引优化
  UNIQUE KEY uniq_user_group (user_group),
  KEY idx_status (status),
  KEY idx_updated_at (updated_at)
) COMMENT='分组实时状态表' ENGINE=InnoDB;
```

#### 2.1.2 新增表：`group_status_history` — 分组状态历史表

```sql
CREATE TABLE group_status_history (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_group VARCHAR(64) NOT NULL,
  
  -- 时间点信息（按小时汇总）
  snapshot_hour BIGINT NOT NULL COMMENT '该小时的起始Unix时间戳',
  
  -- 该小时的指标
  success_rate DECIMAL(5,2),
  avg_response_time INT,
  available_channels INT,
  error_count INT,
  total_requests INT COMMENT '该小时的总请求数',
  
  created_at BIGINT NOT NULL,
  
  KEY idx_user_group_hour (user_group, snapshot_hour),
  KEY idx_snapshot_hour (snapshot_hour)
) COMMENT='分组状态按小时汇总历史' ENGINE=InnoDB;
```

#### 2.1.3 新增表：`model_status` — 模型状态表

```sql
CREATE TABLE model_status (
  id INT PRIMARY KEY AUTO_INCREMENT,
  model_name VARCHAR(255) NOT NULL COMMENT '模型标识',
  display_name VARCHAR(255) COMMENT '显示名称',
  
  -- 状态
  status TINYINT DEFAULT 1 COMMENT '1=健康, 2=警告, 3=不健康, 4=不可用',
  last_test_time BIGINT,
  success_rate DECIMAL(5,2) DEFAULT 100,
  avg_response_time INT,
  
  -- 提供商信息
  provider_type VARCHAR(64) COMMENT 'openai, claude, gemini等',
  
  -- 渠道覆盖情况
  total_channels INT DEFAULT 0 COMMENT '支持该模型的渠道总数',
  available_channels INT DEFAULT 0 COMMENT '可用渠道数',
  
  -- 性能对比
  p50_response_time INT,
  p95_response_time INT,
  
  -- 计费信息快照
  price_per_1k_tokens DECIMAL(12,6) COMMENT '每1000token的价格',
  
  -- 使用统计
  total_usage_tokens INT DEFAULT 0 COMMENT '累计使用token数',
  today_usage_tokens INT DEFAULT 0 COMMENT '今日使用token数',
  
  created_at BIGINT NOT NULL,
  updated_at BIGINT NOT NULL,
  
  UNIQUE KEY uniq_model_name (model_name),
  KEY idx_status (status),
  KEY idx_provider_type (provider_type)
) COMMENT='模型实时状态表' ENGINE=InnoDB;
```

#### 2.1.4 新增表：`model_status_history` — 模型状态历史表

```sql
CREATE TABLE model_status_history (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  model_name VARCHAR(255) NOT NULL,
  snapshot_hour BIGINT NOT NULL,
  
  success_rate DECIMAL(5,2),
  avg_response_time INT,
  available_channels INT,
  error_count INT,
  total_requests INT,
  
  created_at BIGINT NOT NULL,
  
  KEY idx_model_hour (model_name, snapshot_hour)
) COMMENT='模型状态按小时汇总历史' ENGINE=InnoDB;
```

#### 2.1.5 新增表：`user_alerts` — 用户提醒配置表

```sql
CREATE TABLE user_alerts (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  
  -- 提醒规则
  alert_type VARCHAR(64) NOT NULL COMMENT '类型: group_unhealthy, model_unavailable, quota_warning等',
  target_type VARCHAR(64) COMMENT '目标类型: group, model',
  target_name VARCHAR(255) COMMENT '目标: 分组名或模型名',
  
  -- 触发条件
  threshold_type VARCHAR(64) COMMENT 'success_rate, response_time, error_count',
  threshold_value INT COMMENT '阈值',
  
  -- 配置
  enabled TINYINT DEFAULT 1,
  notify_methods VARCHAR(255) DEFAULT 'email,in_app' COMMENT '通知方式',
  
  created_at BIGINT NOT NULL,
  updated_at BIGINT NOT NULL,
  
  KEY idx_user_id (user_id),
  KEY idx_alert_type (alert_type)
) COMMENT='用户提醒规则配置' ENGINE=InnoDB;
```

#### 2.1.6 新增表：`alert_notifications` — 提醒历史记录表

```sql
CREATE TABLE alert_notifications (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  alert_rule_id INT,
  
  -- 提醒内容
  title VARCHAR(255),
  message TEXT,
  alert_type VARCHAR(64),
  target_name VARCHAR(255),
  
  -- 建议
  recommendation TEXT COMMENT '优化建议',
  
  -- 状态
  status VARCHAR(32) DEFAULT 'unread' COMMENT 'unread, read, dismissed, actioned',
  action_taken VARCHAR(255) COMMENT '采取的行动: switch_group, retry_model等',
  
  created_at BIGINT NOT NULL,
  read_at BIGINT,
  
  KEY idx_user_id_created (user_id, created_at),
  KEY idx_status (status)
) COMMENT='提醒通知历史' ENGINE=InnoDB;
```

---

### 2.2 后端API设计

#### 2.2.1 分组状态接口

**GET `/api/group/status` — 获取用户的分组状态列表**

```json
Request: 无

Response: {
  "success": true,
  "data": [
    {
      "group_name": "default",
      "status": 1,  // 1=健康, 2=警告, 3=不健康, 4=不可用
      "success_rate": 99.5,
      "avg_response_time": 245,
      "available_channels": 15,
      "total_channels": 16,
      "p95_response_time": 850,
      "last_test_time": 1717315200,
      "quota_multiplier": 1.0,
      "is_auto_group": false,
      
      // 新增：推荐信息
      "recommendation": {
        "title": "建议切换到高性能分组",
        "reason": "当前分组response_time上升20%，高性能分组可以降低延迟",
        "alternative_group": "premium",
        "expected_improvement": "延迟降低约30%"
      }
    },
    ...
  ]
}
```

**GET `/api/group/:group_name/status/history` — 获取分组历史数据**

```json
Request: ?days=7&interval=hourly  // 7天数据，按小时聚合

Response: {
  "success": true,
  "data": {
    "group_name": "default",
    "history": [
      {
        "timestamp": 1717315200,
        "success_rate": 99.5,
        "avg_response_time": 245,
        "available_channels": 15,
        "error_count": 2
      },
      ...
    ]
  }
}
```

**GET `/api/group/:group_name/channels/status` — 获取分组下各渠道的状态**

```json
Response: {
  "success": true,
  "data": {
    "group_name": "default",
    "channels": [
      {
        "channel_id": 1,
        "channel_name": "OpenAI-Key1",
        "status": 1,
        "success_rate": 100,
        "avg_response_time": 234,
        "last_test_time": 1717315200,
        "models_supported": ["gpt-4o", "gpt-4-turbo", "gpt-3.5-turbo"],
        "status_reason": null
      },
      ...
    ]
  }
}
```

#### 2.2.2 模型状态接口

**GET `/api/models/status` — 获取所有模型状态**

```json
Response: {
  "success": true,
  "data": [
    {
      "model_name": "gpt-4o",
      "status": 1,
      "success_rate": 98.5,
      "avg_response_time": 1240,
      "provider_type": "openai",
      "available_channels": 8,
      "total_channels": 10,
      "price_per_1k_tokens": 0.015,
      "today_usage_tokens": 125000,
      "p95_response_time": 3200,
      
      // 对比和推荐
      "alternatives": [
        {
          "model_name": "gpt-4-turbo",
          "reason": "相同功能，但响应快30%，价格便宜20%",
          "avg_response_time": 850,
          "price_per_1k_tokens": 0.012
        }
      ]
    },
    ...
  ]
}
```

**GET `/api/models/:model_name/status/history` — 获取模型历史数据**

```json
Request: ?days=7

Response: {
  "success": true,
  "data": {
    "model_name": "gpt-4o",
    "history": [
      {
        "timestamp": 1717315200,
        "success_rate": 98.5,
        "avg_response_time": 1240,
        "available_channels": 8,
        "error_count": 12
      },
      ...
    ]
  }
}
```

#### 2.2.3 提醒和建议接口

**GET `/api/alerts` — 获取用户的提醒**

```json
Request: ?status=unread&limit=20

Response: {
  "success": true,
  "data": {
    "unread_count": 3,
    "alerts": [
      {
        "id": 101,
        "type": "group_unhealthy",
        "title": "分组 'default' 状态下降",
        "message": "最近1小时成功率从99.5%下降到95%，建议检查",
        "target_name": "default",
        "created_at": 1717315200,
        "status": "unread",
        
        // 建议行动
        "recommendations": [
          {
            "action": "switch_group",
            "group_name": "premium",
            "reason": "premium分组当前成功率99.8%"
          },
          {
            "action": "switch_model",
            "model_name": "gpt-4-turbo",
            "reason": "gpt-4-turbo在当前分组中性能更好"
          }
        ]
      },
      ...
    ]
  }
}
```

**POST `/api/alerts/:alert_id/mark_read` — 标记提醒为已读**

```json
Request: {
  "action_taken": "switch_group",  // 可选
  "action_target": "premium"        // 可选
}

Response: {
  "success": true
}
```

**POST `/api/alerts/preferences` — 配置提醒偏好**

```json
Request: {
  "alert_types": ["group_unhealthy", "model_unavailable"],
  "enabled": true,
  "notify_methods": ["in_app", "email"],
  "thresholds": {
    "success_rate_warning": 95,  // 低于95%触发警告
    "response_time_warning": 3000  // 超过3000ms触发警告
  }
}

Response: {
  "success": true
}
```

#### 2.2.4 推荐接口

**GET `/api/group/recommend` — 获取最优分组推荐**

```json
Request: ?for_model=gpt-4o  // 可选，为指定模型推荐

Response: {
  "success": true,
  "data": {
    "recommended_groups": [
      {
        "rank": 1,
        "group_name": "premium",
        "score": 95,  // 综合评分
        "reasons": [
          "可用性最高 (99.8%)",
          "响应最快 (平均850ms)",
          "成本性价比高 (倍率1.2倍)"
        ],
        "metrics": {
          "success_rate": 99.8,
          "avg_response_time": 850,
          "quota_multiplier": 1.2
        }
      },
      ...
    ]
  }
}
```

**GET `/api/model/recommend` — 获取最优模型推荐**

```json
Request: ?for_use_case=general_chat  // 可选，指定使用场景

Response: {
  "success": true,
  "data": {
    "recommended_models": [
      {
        "rank": 1,
        "model_name": "gpt-4o",
        "reason": "最佳平衡性能和成本",
        "metrics": {
          "success_rate": 99.8,
          "avg_response_time": 1240,
          "cost_per_1k": 0.015
        }
      },
      {
        "rank": 2,
        "model_name": "gpt-4-turbo",
        "reason": "成本更低，性能相近",
        "metrics": {
          "success_rate": 99.5,
          "avg_response_time": 1100,
          "cost_per_1k": 0.012
        }
      }
    ]
  }
}
```

---

### 2.3 后端逻辑设计

#### 2.3.1 定时任务：状态汇总任务（每小时执行）

**触发条件**：每小时的第5分钟  
**功能**：将该小时内的测试数据聚合成历史记录

```go
func StartGroupStatusAggregationTask() {
    // 每小时执行一次（在每小时的第5分钟）
    ticker := time.NewTicker(1 * time.Hour)
    defer ticker.Stop()
    
    go func() {
        // 首次启动时，等待到下一个整点+5分钟
        initialWait := calculateWaitTime()
        time.Sleep(initialWait)
        
        for range ticker.C {
            aggregateGroupStatusHourly()
            aggregateModelStatusHourly()
            cleanupOldHistory()  // 保留最近90天数据
        }
    }()
}

func aggregateGroupStatusHourly() {
    // 1. 查询consume_log中最近1小时的记录
    logs := queryConsumeLogsLastHour()
    
    // 2. 按group分组统计
    for group, logs := range groupByGroup(logs) {
        metrics := calculateMetrics(logs)
        
        // 3. 更新group_status表
        updateGroupStatus(group, metrics)
        
        // 4. 插入group_status_history表
        insertGroupStatusHistory(group, metrics)
        
        // 5. 检查并生成告警
        checkAndGenerateAlerts(group, metrics)
    }
}
```

#### 2.3.2 定时任务：渠道状态扫描（与现有自动测试集成）

**修改现有逻辑**：在 `testAllChannels()` 完成后更新group_status表

```go
func testAllChannels(checkUpstream bool) error {
    // ... 现有的逻辑 ...
    
    // 测试完成后，更新分组状态
    defer func() {
        aggregateGroupStatusRealtime()
    }()
}

func aggregateGroupStatusRealtime() {
    // 立即基于最新的consume_log更新group_status
    groups := getAllGroups()
    for _, group := range groups {
        metrics := calculateMetricsForGroup(group)
        updateGroupStatus(group, metrics)
    }
}
```

#### 2.3.3 定时任务：智能提醒任务（每5分钟检查一次）

```go
func StartAlertsGenerationTask() {
    ticker := time.NewTicker(5 * time.Minute)
    defer ticker.Stop()
    
    go func() {
        for range ticker.C {
            checkGroupHealthAlerts()
            checkModelAvailabilityAlerts()
            checkQuotaWarningAlerts()
            checkPerformanceDegradationAlerts()
        }
    }()
}

// 检查分组不健康告警
func checkGroupHealthAlerts() {
    groups := getAllGroupStatus()
    for _, group := range groups {
        if group.SuccessRate < threshold && !hasRecentAlert(group.Name, "group_unhealthy") {
            // 生成告警
            alert := createAlert(group)
            
            // 获取推荐
            recommendations := generateRecommendations(group)
            
            // 通知用户
            notifyUsers(group.UserID, alert, recommendations)
        }
    }
}

// 生成推荐
func generateRecommendations(group *GroupStatus) []Recommendation {
    recs := make([]Recommendation, 0)
    
    // 推荐1: 切换到更好的分组
    if betterGroup := findBetterGroup(group); betterGroup != nil {
        recs = append(recs, Recommendation{
            Action: "switch_group",
            Target: betterGroup.Name,
            Reason: fmt.Sprintf("该分组成功率%.1f%%，更高", betterGroup.SuccessRate),
        })
    }
    
    // 推荐2: 切换到不受影响的模型
    if altModel := findBetterModel(group); altModel != nil {
        recs = append(recs, Recommendation{
            Action: "switch_model",
            Target: altModel.Name,
            Reason: fmt.Sprintf("该模型在此分组中表现更好", altModel.AvgResponseTime),
        })
    }
    
    return recs
}
```

#### 2.3.4 状态计算逻辑

```go
type StatusMetrics struct {
    SuccessRate         float64  // 成功率 = 成功数 / 总数
    AvgResponseTime     int      // 平均响应时间
    P50ResponseTime     int      // 中位数
    P95ResponseTime     int      // 95分位数
    ErrorCount          int      // 错误数
    TotalRequests       int      // 总请求数
    AvailableChannels   int      // 可用渠道数
    DisabledChannels    int      // 禁用渠道数
}

func calculateMetrics(logs []ConsumeLog) StatusMetrics {
    successCount := 0
    totalCount := len(logs)
    responseTimes := make([]int, 0, totalCount)
    
    for _, log := range logs {
        responseTimes = append(responseTimes, log.ResponseTime)
        if log.Status == SUCCESS {
            successCount++
        }
    }
    
    sort.Ints(responseTimes)
    
    return StatusMetrics{
        SuccessRate:     float64(successCount) / float64(totalCount) * 100,
        AvgResponseTime: averageInt(responseTimes),
        P50ResponseTime: percentile(responseTimes, 50),
        P95ResponseTime: percentile(responseTimes, 95),
        ErrorCount:      totalCount - successCount,
        TotalRequests:   totalCount,
    }
}

// 状态分级判断
func determineStatus(metrics StatusMetrics) int {
    if metrics.SuccessRate >= 99 && metrics.P95ResponseTime < 2000 {
        return 1  // 健康
    } else if metrics.SuccessRate >= 95 && metrics.P95ResponseTime < 3000 {
        return 2  // 警告
    } else if metrics.SuccessRate >= 90 {
        return 3  // 不健康
    }
    return 4  // 不可用
}
```

---

### 2.4 前端UI设计

#### 2.4.1 仪表盘新增卡片：分组和模型状态卡片

**位置**：仪表盘首页，显著位置（现有图表下方）

**卡片1：分组健康状态**
```
╔════════════════════════════════════════════╗
║ 分组状态概览                  更新: 5分钟前   ║
╠════════════════════════════════════════════╣
║                                            ║
║ 默认分组 (default)            🟢 健康        ║
║ 成功率: 99.5% | 平均响应: 245ms | 可用: 15/16 ║
║                                            ║
║ 高性能分组 (premium)          🟢 健康        ║
║ 成功率: 99.8% | 平均响应: 180ms | 可用: 8/8  ║
║ 💡 建议: 此分组性能更优，建议切换          ║
║                                            ║
║ 测试分组 (test)               🟡 警告        ║
║ 成功率: 94.2% | 平均响应: 523ms | 可用: 12/16║
║ 🔔 警告: 成功率低于95%                     ║
║                                            ║
╚════════════════════════════════════════════╝
```

**交互**：
- 点击分组名 → 展开详细信息（24h/7d/30d趋势图）
- 点击"建议" → 显示切换提示
- 状态图标悬停 → 显示详细状态说明

**卡片2：模型状态排行**
```
╔════════════════════════════════════════════╗
║ 模型性能排行              [切换视图]        ║
╠════════════════════════════════════════════╣
║                                            ║
║ 模型          状态  成功率  响应  成本      ║
║ gpt-4o        🟢    99.8%  1.2s  $0.015    ║
║ gpt-4-turbo   🟢    99.5%  1.1s  $0.012 ⭐ ║
║ gpt-3.5-turbo 🟢    98.5%  0.8s  $0.002 💰 ║
║ claude-3      🟡    95.2%  2.3s  $0.008    ║
║                                            ║
║ [按成本排序] [按速度排序] [按可靠性排序]    ║
╚════════════════════════════════════════════╝
```

#### 2.4.2 分组管理页面新增内容

**路由**：`/dashboard/groups` 或 `/_authenticated/groups`

**页面布局**：
```
┌─────────────────────────────────────────────────┐
│ 分组管理                                        │
├─────────────────────────────────────────────────┤
│                                                 │
│ [分组列表]  [性能对比]  [建议]                   │
│                                                 │
│ 📊 分组详情表格                                  │
│ ┌───────────┬────────┬────────┬─────────────┐  │
│ │分组名     │状态    │成功率  │平均响应    │  │
│ ├───────────┼────────┼────────┼─────────────┤  │
│ │default    │🟢健康  │99.5%   │245ms        │  │
│ │premium    │🟢健康  │99.8%   │180ms 推荐✨ │  │
│ │test       │🟡警告  │94.2%   │523ms        │  │
│ └───────────┴────────┴────────┴─────────────┘  │
│                                                 │
│ 📈 选定分组的性能趋势 (7天)                      │
│ [折线图: 成功率/响应时间/可用渠道]               │
│                                                 │
│ 💡 智能建议                                     │
│ • default分组：当前表现良好，无需调整          │
│ • 可选：切换到premium分组以获得更快响应        │
│                                                 │
└─────────────────────────────────────────────────┘
```

#### 2.4.3 用户令牌(API Keys)页面新增列

**修改**：`api-keys-columns.tsx` 添加新列

```typescript
{
  header: "使用分组",
  accessorKey: "group",
  cell: ({ row }) => {
    const group = row.getValue("group");
    const status = getGroupStatus(group);
    
    return (
      <div className="flex items-center gap-2">
        <StatusBadge status={status} />
        <span>{group}</span>
      </div>
    );
  }
},
{
  header: "分组健康度",
  accessorKey: "group_health",
  cell: ({ row }) => {
    const group = row.getValue("group");
    const metrics = getGroupMetrics(group);
    
    return (
      <div className="flex items-center gap-1">
        <ProgressBar value={metrics.successRate} max={100} />
        <span className="text-xs">{metrics.successRate.toFixed(1)}%</span>
      </div>
    );
  }
},
{
  header: "建议",
  accessorKey: "recommendation",
  cell: ({ row }) => {
    const group = row.getValue("group");
    const rec = getRecommendation(group);
    
    if (!rec) return null;
    
    return (
      <Tooltip title={rec.reason}>
        <Button
          size="sm"
          variant="ghost"
          onClick={() => switchGroup(rec.targetGroup)}
        >
          💡 切换到 {rec.targetGroup}
        </Button>
      </Tooltip>
    );
  }
}
```

#### 2.4.4 模型广场(Pricing)页面增强

**修改**：在模型卡片中添加状态信息

```typescript
// model-card.tsx 中增加
<div className="mb-3 pb-3 border-b">
  <div className="flex items-center justify-between mb-2">
    <span className="text-sm font-medium">状态</span>
    <StatusBadge status={model.status} />
  </div>
  
  <div className="grid grid-cols-2 gap-2 text-xs">
    <div>
      <span className="text-gray-600">成功率</span>
      <div className="font-semibold">
        {model.successRate.toFixed(1)}%
      </div>
    </div>
    <div>
      <span className="text-gray-600">平均响应</span>
      <div className="font-semibold">
        {model.avgResponseTime}ms
      </div>
    </div>
  </div>
  
  {/* 显示更好的替代方案 */}
  {model.alternatives && (
    <div className="mt-3 p-2 bg-blue-50 rounded">
      <p className="text-xs font-medium mb-1">🔄 性能对标</p>
      {model.alternatives.map(alt => (
        <div key={alt.modelName} className="text-xs mb-1">
          <strong>{alt.modelName}</strong>: {alt.reason}
          <button 
            className="ml-1 text-blue-600 underline"
            onClick={() => switchModel(alt.modelName)}
          >
            使用此模型
          </button>
        </div>
      ))}
    </div>
  )}
</div>
```

#### 2.4.5 新增提醒中心页面

**路由**：`/_authenticated/alerts` 或 `dashboard/alerts`

```
┌────────────────────────────────────────────┐
│ 通知中心                [设置]  [标记全部已读] │
├────────────────────────────────────────────┤
│                                            │
│ [全部] [分组] [模型] [配额] [未读 3]        │
│                                            │
│ 🔴 [new] 分组 'default' 状态下降           │
│ 最近1小时成功率从99.5%下降到95%，建议检查 │
│ 建议：                                      │
│ • 切换到 'premium' 分组 (成功率99.8%)      │
│ • 检查当前分组的渠道配置                  │
│ [✓ 已处理] [☐ 忽略] [更多信息]             │
│ 创建于: 10分钟前                           │
│                                            │
│ ─────────────────────────────────────────  │
│                                            │
│ 🟡 [new] 模型 'gpt-4' 可用渠道减少         │
│ 从8个减少到6个，建议使用替代模型          │
│ [✓ 已处理] [☐ 忽略] [更多信息]             │
│ 创建于: 25分钟前                           │
│                                            │
└────────────────────────────────────────────┘
```

**提醒偏好设置**：
```
┌────────────────────────────────────────────┐
│ 通知偏好设置                               │
├────────────────────────────────────────────┤
│                                            │
│ 启用通知 [切换开关]                        │
│                                            │
│ 通知方式:                                  │
│ ☑ 应用内通知  ☑ 邮件  ☐ 短信              │
│                                            │
│ 告警阈值:                                  │
│ • 成功率低于: [_95_]%                      │
│ • 响应时间超过: [_3000_]ms                 │
│ • 错误次数超过: [_10_]/小时                │
│                                            │
│ 告警类型:                                  │
│ ☑ 分组不健康  ☑ 模型不可用                │
│ ☑ 配额警告    ☑ 性能下降                 │
│                                            │
│ [保存设置]                                 │
│                                            │
└────────────────────────────────────────────┘
```

---

### 2.5 前端组件清单

**新增组件**：
- `StatusBadge` - 状态徽章（健康/警告/不健康/不可用）
- `MetricsChart` - 指标趋势图表
- `GroupStatusCard` - 分组状态卡片
- `ModelStatusCard` - 模型状态卡片
- `AlertNotification` - 单个通知组件
- `AlertCenter` - 通知中心容器
- `RecommendationPanel` - 推荐建议面板
- `PerformanceComparison` - 性能对比表格

**修改的组件**：
- `api-keys-table.tsx` - 添加分组状态列
- `api-keys-columns.tsx` - 添加新列定义
- `model-card.tsx` - 显示模型状态和替代方案
- `overview-dashboard.tsx` - 添加状态卡片

---

## 3. 实现路线图

### 第一阶段：基础数据层（1-2周）

**目标**：建立数据模型和API框架

1. ✅ 创建6个新数据表（GORM model）
   - `GroupStatus`, `GroupStatusHistory`
   - `ModelStatus`, `ModelStatusHistory`
   - `UserAlerts`, `AlertNotifications`

2. ✅ 创建Service层
   - `GroupStatusService` - 分组状态CRUD和计算
   - `ModelStatusService` - 模型状态CRUD和计算
   - `AlertService` - 提醒管理

3. ✅ 创建Controller层和路由
   - GET `/api/group/status`
   - GET `/api/group/:group/status/history`
   - GET `/api/models/status`
   - GET `/api/alerts`
   - POST `/api/alerts/:id/mark_read`

### 第二阶段：定时任务（1-2周）

**目标**：实现自动化数据采集和聚合

1. ✅ 实现状态聚合任务（每小时）
   - 从consume_log聚合数据
   - 更新GroupStatus/ModelStatus表
   - 插入历史记录

2. ✅ 实现告警生成任务（每5分钟）
   - 检查分组/模型健康度
   - 生成AlertNotifications
   - 智能推荐逻辑

3. ✅ 集成到现有自动测试
   - 修改testAllChannels()
   - 实时更新分组状态

### 第三阶段：前端UI（2-3周）

**目标**：用户可见的状态展示和交互

1. ✅ 仪表盘卡片
   - 分组状态卡片
   - 模型排行卡片
   - 集成趋势图表

2. ✅ 专用管理页面
   - `/dashboard/groups` 分组管理
   - `/_authenticated/alerts` 通知中心

3. ✅ 现有页面增强
   - API Keys页面添加分组状态列
   - 模型广场添加状态和推荐

4. ✅ 通知呈现
   - 应用内通知浮窗
   - 邮件通知模板

### 第四阶段：推荐引擎（1-2周）

**目标**：智能决策支持

1. ✅ 分组推荐
   - 基于用户的使用模式
   - 基于当前分组的问题
   - 计算推荐分数

2. ✅ 模型推荐
   - 功能相近的模型对比
   - 成本-性能权衡
   - 可用性考虑

3. ✅ 推荐API
   - GET `/api/group/recommend`
   - GET `/api/model/recommend`

### 第五阶段：优化和测试（1周）

**目标**：性能优化、数据准确性、用户体验

1. ✅ 数据库优化
   - 索引优化
   - 历史数据分区/归档
   - 查询性能测试

2. ✅ 计算优化
   - 缓存热点数据
   - 异步聚合任务
   - 避免重复计算

3. ✅ 功能测试
   - 告警准确性验证
   - 推荐效果评估
   - 端到端测试

---

## 4. 技术特性

### 4.1 可扩展性

- **模块化**：各功能独立，易于添加新的告警规则或推荐策略
- **可配置**：通过数据库配置告警阈值，无需重新部署
- **可观测**：完整的历史数据，支持事后分析

### 4.2 性能考虑

| 场景 | 优化方案 |
|------|--------|
| 实时查询分组状态 | 缓存group_status表（5分钟TTL） |
| 历史数据查询 | 按日期范围分区history表 |
| 告警生成 | 异步处理，避免阻塞主流程 |
| 推荐计算 | 预计算+缓存（15分钟更新一次） |

### 4.3 数据准确性

- 基于consume_log实际数据计算
- 使用分位数而非平均值（减少离群值影响）
- 定期审计tell警准确率

### 4.4 隐私和权限

- 用户仅看到自己的告警和数据
- 不同角色的数据可见性不同
  - 普通用户：只看自己的分组/API Key
  - 管理员：看全部数据

---

## 5. 示例场景

### 场景1：分组性能下降提醒

```
时间轴:
14:00 - 系统检测到default分组成功率从99.5%下降到95%
14:05 - 生成告警，推荐切换到premium分组
14:10 - 用户收到应用内通知和邮件
14:15 - 用户点击"切换到premium"，系统记录行动
14:20 - 系统验证切换生效，更新通知状态为"已处理"

后续分析:
- 7天后，统计该用户通过建议切换分组的收益
- 反馈给推荐引擎，提升下次推荐的准确率
```

### 场景2：模型替代方案推荐

```
用户当前使用: gpt-4o (响应: 1.2s, 成本: $0.015)

系统发现:
- gpt-4-turbo: 响应快15% (1.0s), 成本便宜20% ($0.012)
- 但gpt-4o在current group中成功率99.8% vs gpt-4-turbo的98.5%

推荐:
- 如果优先考虑成本：使用gpt-4-turbo
- 如果优先考虑可靠性：保持gpt-4o
- 折中方案：在某些请求用gpt-4-turbo，关键请求用gpt-4o
```

### 场景3：自动分组切换

```
用户配置:
- API Key关联到"default"分组
- 启用"自动分组优化"
- 优先级：可靠性 > 性能 > 成本

系统行为:
- 监控default分组的质量
- 当成功率连续2小时<95%时，自动临时切换到premium
- 待成功率恢复后，自动切换回去
- 发送通知告知用户已自动优化

用户收益:
- 不用手动管理，系统自动优化
- 保证高可靠性的同时，节省成本
```

---

## 6. 风险和缓解

| 风险 | 影响 | 缓解方案 |
|------|------|--------|
| 告警阈值设置不当 | 误告/漏告 | 提供默认值+用户可调，初期保守 |
| 推荐不准确 | 用户体验差 | 初期仅推荐，不强制；收集反馈迭代 |
| 数据延迟 | 无法及时响应 | 每5分钟聚合一次，P99延迟<10分钟 |
| 存储爆炸 | 数据库膨胀 | 历史数据按周期自动归档 |

---

## 7. 成功指标

- **用户采纳率**：使用分组推荐的活跃用户占比 >50%
- **告警准确率**：有效告警率 >90%
- **性能改善**：采用推荐的用户，平均响应时间下降 >10%
- **成本优化**：采用推荐的用户，消耗配额减少 >15%
- **用户满意度**：NPS评分 >50

---

## 8. 未来扩展

1. **机器学习集成**
   - 基于用户历史行为预测最优分组
   - 异常检测告警

2. **用户行为分析**
   - 识别用户的使用模式
   - 个性化推荐

3. **SLA监控**
   - 基于用户购买的SLA等级
   - 自动告警和补偿机制

4. **成本优化引擎**
   - 自动识别成本优化机会
   - 推荐模型/分组替换方案

5. **预测性扩容**
   - 基于趋势预测需要的容量
   - 提前安排资源

---

## 附录：API响应示例完整版

### 示例1：获取分组状态

```bash
curl -X GET "https://api.example.com/api/group/status" \
  -H "Authorization: Bearer $TOKEN"
```

```json
{
  "success": true,
  "data": [
    {
      "group_name": "default",
      "status": 1,
      "success_rate": 99.5,
      "avg_response_time": 245,
      "p95_response_time": 850,
      "available_channels": 15,
      "total_channels": 16,
      "disabled_channels": 0,
      "error_count": 2,
      "last_test_time": 1717315200,
      "quota_multiplier": 1.0,
      "is_auto_group": false,
      
      "recommendation": {
        "type": null,
        "title": "分组运行正常",
        "message": "您的default分组表现稳定，无需调整"
      }
    },
    {
      "group_name": "premium",
      "status": 1,
      "success_rate": 99.8,
      "avg_response_time": 180,
      "p95_response_time": 650,
      "available_channels": 8,
      "total_channels": 8,
      "disabled_channels": 0,
      "error_count": 0,
      "last_test_time": 1717315200,
      "quota_multiplier": 1.5,
      "is_auto_group": false,
      
      "recommendation": null
    },
    {
      "group_name": "test",
      "status": 2,
      "success_rate": 94.2,
      "avg_response_time": 523,
      "p95_response_time": 1800,
      "available_channels": 12,
      "total_channels": 16,
      "disabled_channels": 3,
      "error_count": 12,
      "last_test_time": 1717315200,
      "quota_multiplier": 0.8,
      "is_auto_group": false,
      
      "recommendation": {
        "type": "health_warning",
        "title": "分组状态下降",
        "message": "该分组最近成功率低于95%，建议检查或切换到其他分组",
        "suggested_action": "switch_group",
        "suggested_target": "default"
      }
    }
  ]
}
```

### 示例2：获取模型推荐

```bash
curl -X GET "https://api.example.com/api/model/recommend?for_use_case=general_chat" \
  -H "Authorization: Bearer $TOKEN"
```

```json
{
  "success": true,
  "data": {
    "use_case": "general_chat",
    "recommended_models": [
      {
        "rank": 1,
        "model_name": "gpt-4o",
        "display_name": "GPT-4O",
        "provider": "openai",
        "status": 1,
        "score": 98,
        "reasons": [
          "功能最全面，支持所有特性",
          "成功率最高 (99.8%)",
          "响应速度适中 (1.2s)",
          "综合性价比最优"
        ],
        "metrics": {
          "success_rate": 99.8,
          "avg_response_time": 1200,
          "p95_response_time": 3200,
          "price_per_1k_tokens": 0.015,
          "available_channels": 8
        }
      },
      {
        "rank": 2,
        "model_name": "gpt-4-turbo",
        "display_name": "GPT-4 Turbo",
        "provider": "openai",
        "status": 1,
        "score": 92,
        "reasons": [
          "速度快15%，成本便宜20%",
          "适合对延迟敏感的应用",
          "功能与gpt-4o相近"
        ],
        "metrics": {
          "success_rate": 99.5,
          "avg_response_time": 1000,
          "p95_response_time": 2800,
          "price_per_1k_tokens": 0.012,
          "available_channels": 6
        },
        "vs_recommended": {
          "speed_improvement": 16.7,  // %
          "cost_saving": 20,  // %
          "reliability_diff": -0.3  // %
        }
      },
      {
        "rank": 3,
        "model_name": "gpt-3.5-turbo",
        "display_name": "GPT-3.5 Turbo",
        "provider": "openai",
        "status": 1,
        "score": 78,
        "reasons": [
          "成本最低，响应最快",
          "适合简单对话",
          "性能与gpt-4o有较大差距"
        ],
        "metrics": {
          "success_rate": 98.5,
          "avg_response_time": 800,
          "p95_response_time": 2200,
          "price_per_1k_tokens": 0.002,
          "available_channels": 12
        }
      }
    ]
  }
}
```

### 示例3：获取告警通知

```bash
curl -X GET "https://api.example.com/api/alerts?status=unread&limit=10" \
  -H "Authorization: Bearer $TOKEN"
```

```json
{
  "success": true,
  "data": {
    "unread_count": 3,
    "total_count": 24,
    "alerts": [
      {
        "id": 1001,
        "type": "group_unhealthy",
        "severity": "warning",
        "title": "分组 'default' 健康度下降",
        "message": "最近1小时成功率从99.5%下降到95.2%，请注意",
        "target_type": "group",
        "target_name": "default",
        
        "metrics": {
          "previous_success_rate": 99.5,
          "current_success_rate": 95.2,
          "change_percentage": -4.3,
          "avg_response_time": 523,
          "disabled_channels": 3
        },
        
        "recommendations": [
          {
            "action": "switch_group",
            "target": "premium",
            "reason": "premium分组当前成功率99.8%，响应时间更快",
            "estimated_improvement": {
              "success_rate_improvement": 4.6,
              "response_time_improvement": -258,
              "cost_multiplier": 1.5
            }
          },
          {
            "action": "check_channels",
            "reason": "建议检查该分组下的被禁用渠道，可能存在配置问题",
            "details": ["OpenAI-Key2", "Claude-API", "Gemini-Standard"]
          }
        ],
        
        "status": "unread",
        "created_at": 1717318200,
        "action_taken": null
      },
      {
        "id": 1002,
        "type": "model_unavailable",
        "severity": "info",
        "title": "模型 'claude-3-sonnet' 可用渠道减少",
        "message": "该模型的可用渠道从5个减少到3个",
        "target_type": "model",
        "target_name": "claude-3-sonnet",
        
        "metrics": {
          "previous_channel_count": 5,
          "current_channel_count": 3,
          "unavailable_channels": ["Claude-Premium-2", "Claude-Batch"]
        },
        
        "recommendations": [
          {
            "action": "switch_model",
            "target": "claude-3-haiku",
            "reason": "相同功能，可用渠道更多 (8个)，性能相近",
            "estimated_improvement": {
              "reliability": "+60%",
              "cost_reduction": "-30%"
            }
          }
        ],
        
        "status": "unread",
        "created_at": 1717316800,
        "action_taken": null
      }
    ]
  }
}
```

---

**文档版本历史**：
- v1.0 (2026-06-02) - 初版设计方案，包含所有核心功能和实现路线图

