# 自动分组/模型状态监控 — 快速参考与检查清单

**快速导航指南** | **版本**: 1.0 | **日期**: 2026-06-02

---

## 一、项目文档速查表

| 文档 | 内容 | 适用场景 |
|------|------|--------|
| **功能方案-自动分组模型状态监控与提醒系统.md** | 完整的功能设计、数据模型、API设计、前端UI设计 | 了解整体功能需求、审查设计方案 |
| **实现架构-代码框架与集成方案.md** | 系统架构图、数据流、代码框架、定时任务集成 | 进行代码实现、集成到现有系统 |
| **快速参考-实现检查清单.md** (本文档) | 检查清单、常见问题、快速命令、关键路径 | 快速查找信息、推进实现进度 |

---

## 二、关键路径速览

### 2.1 前端展示需要修改的位置

```
仪表盘 (dashboard)
├─ src/routes/_authenticated/dashboard/index.tsx
│  └─ 【新增】分组状态卡片、模型排行卡片
│
API Keys 页面 (keys)
├─ src/features/keys/components/api-keys-columns.tsx
│  └─ 【修改】添加"使用分组"列和"分组健康度"列
│
模型广场 (pricing)
├─ src/features/pricing/components/model-card.tsx
│  └─ 【修改】显示模型状态、替代方案提示
│
【新增】通知中心
└─ src/routes/_authenticated/alerts/index.tsx
   └─ 【新增】完整的告警展示和处理界面
```

### 2.2 后端代码需要新增/修改的位置

```
Service 层 (service/)
├─ group_status.go  【新增】
│  ├─ GetUserGroupStatus()
│  ├─ CalculateGroupMetrics()
│  ├─ UpdateGroupStatus()
│  └─ RecommendBetterGroups()
│
├─ alert_service.go  【新增】
│  ├─ CheckGroupHealthAlerts()
│  ├─ MarkAlertAsRead()
│  └─ GetUserUnreadAlerts()
│
└─ model_status_service.go  【新增】（与群组类似）

Controller 层 (controller/)
├─ group_status.go  【新增】
│  ├─ GetGroupStatus()
│  ├─ GetGroupStatusHistory()
│  └─ RecommendGroups()
│
├─ alert.go  【新增】
│  ├─ GetAlerts()
│  ├─ MarkAlertAsRead()
│  └─ SetAlertPreferences()
│
└─ channel_test.go  【修改】
   └─ testAllChannels() - 末尾调用 aggregateGroupStatusRealtime()

Model 层 (model/)
├─ group_status.go  【新增】- GORM model
├─ group_status_history.go  【新增】
├─ model_status.go  【新增】
├─ model_status_history.go  【新增】
├─ alert_notification.go  【新增】
└─ user_alerts.go  【新增】

Router 层 (router/)
└─ api-router.go  【修改】
   ├─ /api/group/status
   ├─ /api/group/:group/status/history
   ├─ /api/models/status
   ├─ /api/alerts
   └─ /api/alerts/:id/mark_read

Main (main.go)  【修改】
├─ StartGroupStatusAggregationTask()  【新增】
└─ StartAlertsGenerationTask()  【新增】
```

### 2.3 数据库需要新增的表

```
group_status          - 分组实时状态表
group_status_history  - 分组历史状态表
model_status          - 模型实时状态表
model_status_history  - 模型历史状态表
user_alerts           - 用户告警规则配置表
alert_notifications   - 告警通知历史表
```

---

## 三、实现检查清单

### 阶段1：后端基础设施 (1-2周)

- [ ] **数据模型**
  - [ ] 创建 `model/group_status.go` (GroupStatus struct)
  - [ ] 创建 `model/group_status_history.go`
  - [ ] 创建 `model/model_status.go`
  - [ ] 创建 `model/model_status_history.go`
  - [ ] 创建 `model/alert_notification.go`
  - [ ] 创建 `model/user_alerts.go`
  - [ ] 在 `model/migration.go` 中添加表迁移

- [ ] **Service 层**
  - [ ] 创建 `service/group_status_service.go`
    - [ ] GetUserGroupStatus()
    - [ ] CalculateGroupMetrics() - 从consume_log读取
    - [ ] UpdateGroupStatus()
    - [ ] AggregateHourlyHistory()
    - [ ] RecommendBetterGroups()
  
  - [ ] 创建 `service/model_status_service.go` (类似分组服务)
  
  - [ ] 创建 `service/alert_service.go`
    - [ ] CheckGroupHealthAlerts()
    - [ ] CheckModelAvailabilityAlerts()
    - [ ] generateRecommendations()
    - [ ] MarkAlertAsRead()
    - [ ] GetUserUnreadAlerts()

- [ ] **Controller 层**
  - [ ] 创建 `controller/group_status.go`
    - [ ] GetGroupStatus()
    - [ ] GetGroupStatusHistory()
    - [ ] GetGroupChannelsStatus()
    - [ ] RecommendGroups()
  
  - [ ] 创建 `controller/model_status.go` (类似)
  
  - [ ] 创建 `controller/alert.go`
    - [ ] GetAlerts()
    - [ ] MarkAlertAsRead()
    - [ ] GetAlertPreferences()
    - [ ] SetAlertPreferences()

- [ ] **路由配置**
  - [ ] 在 `router/api-router.go` 中添加6个新路由组
  - [ ] 测试路由可访问性

- [ ] **定时任务**
  - [ ] 在 `main.go` 中添加 StartGroupStatusAggregationTask()
  - [ ] 在 `main.go` 中添加 StartAlertsGenerationTask()
  - [ ] 验证定时任务启动

### 阶段2：与现有自动测试的集成 (1周)

- [ ] **修改自动测试逻辑**
  - [ ] 打开 `controller/channel_test.go`
  - [ ] 在 testAllChannels() 的末尾添加 aggregateGroupStatusRealtime() 调用
  - [ ] 测试修改后的自动测试流程

- [ ] **数据准确性验证**
  - [ ] 验证消费日志中有正确的group信息
  - [ ] 验证metrics计算逻辑正确
  - [ ] 对比历史数据的准确性

### 阶段3：前端UI开发 (2-3周)

**仪表盘 (dashboard)**
- [ ] 创建 `GroupStatusCard` 组件
- [ ] 创建 `ModelStatusCard` 组件
- [ ] 创建 `StatusBadge` 组件（显示 🟢健康 🟡警告 🔴不健康）
- [ ] 创建 `PerformanceTrendChart` 组件（折线图展示趋势）
- [ ] 在 dashboard/index.tsx 中集成这些卡片
- [ ] 样式调整，确保与现有设计一致

**API Keys 页面**
- [ ] 修改 `api-keys-columns.tsx`
  - [ ] 添加"使用分组"列
  - [ ] 添加"分组健康度"列（成功率进度条）
  - [ ] 添加"建议"列（切换按钮）
- [ ] 测试列显示和排序

**模型广场 (pricing)**
- [ ] 修改 `model-card.tsx`
  - [ ] 添加状态徽章
  - [ ] 添加成功率/响应时间显示
  - [ ] 添加替代方案推荐框
- [ ] 测试卡片显示

**新增：通知中心**
- [ ] 创建 `src/routes/_authenticated/alerts/index.tsx`
- [ ] 创建 `AlertCenter` 容器组件
- [ ] 创建 `AlertItem` 展示单个告警
- [ ] 创建 `AlertPreferences` 设置面板
- [ ] 集成API调用获取告警列表
- [ ] 实现"标记已读"、"更多信息"、"采取行动"等功能

**新增页面链接**
- [ ] 在导航菜单中添加"通知中心"链接
- [ ] 仪表盘中添加"查看全部告警"链接
- [ ] 显示未读通知徽章（红点）

### 阶段4：推荐引擎 (1-2周)

- [ ] **分组推荐算法**
  - [ ] RecommendBetterGroups() - 找出性能更好的分组
  - [ ] 实现推荐分数计算
  - [ ] GET `/api/group/recommend` API

- [ ] **模型推荐算法**
  - [ ] RecommendBetterModels() - 找出功能相近、性能更优的模型
  - [ ] 成本-性能权衡计算
  - [ ] GET `/api/model/recommend` API

- [ ] **前端集成**
  - [ ] 模型卡片显示替代方案
  - [ ] API Keys页面显示切换建议
  - [ ] 告警中显示推荐行动

### 阶段5：测试与优化 (1周)

- [ ] **功能测试**
  - [ ] 分组状态计算准确性
  - [ ] 告警生成的及时性和准确性
  - [ ] 推荐的有效性

- [ ] **性能优化**
  - [ ] 实现缓存（GroupStatus 5分钟TTL）
  - [ ] 数据库查询优化（索引检查）
  - [ ] 定时任务性能监控

- [ ] **用户体验**
  - [ ] UI响应速度
  - [ ] 通知提示的清晰性
  - [ ] 推荐行动的可操作性

---

## 四、常见问题速查

### Q1: 分组状态什么时候更新?

**A**: 有两种更新时机：
1. **实时更新** - 每次自动测试后（依赖现有AutomaticallyTestChannels）
2. **小时汇总** - 每小时5分钟执行，聚合该小时的consume_log数据

### Q2: 告警如何避免风暴？

**A**: 实现了防抖机制：
```
- 同一个告警6小时内仅生成一次
- 设置了告警阈值（成功率<95%, 响应时间>3000ms）
- 用户可以自定义阈值
```

### Q3: 如何处理用户的某个API Key关联多个分组?

**A**: 目前设计中每个API Key只能关联一个分组。如果需要多分组，可以：
1. 为每个分组创建单独的API Key
2. 或在推荐时同时推荐分组和分组内的模型

### Q4: 历史数据保留多久?

**A**: 默认保留90天，可通过配置调整：
```go
// main.go 中的聚合任务
cutoffTime := time.Now().Unix() - 90*24*3600
```

### Q5: 推荐算法如何考虑成本?

**A**: 推荐时计算综合评分：
```
Score = (success_rate_weight * success_rate%) + 
        (speed_weight * (1 - normalized_response_time)) +
        (cost_weight * (1 - normalized_cost_multiplier))
```
权重可配置，默认：可靠性40% + 速度30% + 成本30%

### Q6: 如何测试定时任务?

**A**: 
```bash
# 手动触发（需添加DEBUG接口）
curl -X POST "http://localhost:8080/api/debug/trigger-aggregation"

# 或查看日志
tail -f logs/app.log | grep "aggregate\|alert"
```

---

## 五、数据库快速参考

### 创建表的SQL语句 (参考)

```sql
-- 分组状态
CREATE TABLE group_status (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_group VARCHAR(64) UNIQUE NOT NULL,
  group_name VARCHAR(128),
  status TINYINT DEFAULT 1,
  success_rate DECIMAL(5,2) DEFAULT 100,
  avg_response_time INT,
  available_channels INT DEFAULT 0,
  total_channels INT DEFAULT 0,
  last_test_time BIGINT,
  p95_response_time INT,
  error_count INT DEFAULT 0,
  created_at BIGINT,
  updated_at BIGINT,
  INDEX idx_status (status),
  INDEX idx_updated (updated_at)
);

-- 告警通知
CREATE TABLE alert_notifications (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  title VARCHAR(255),
  message TEXT,
  alert_type VARCHAR(64),
  target_name VARCHAR(255),
  status VARCHAR(32) DEFAULT 'unread',
  recommendation TEXT,
  created_at BIGINT,
  INDEX idx_user_created (user_id, created_at),
  INDEX idx_status (status)
);
```

---

## 六、关键API端点列表

### 分组相关
```
GET    /api/group/status                    获取用户分组状态列表
GET    /api/group/:group/status/history     获取分组历史数据（可指定天数）
GET    /api/group/:group/channels/status    获取分组下各渠道状态
GET    /api/group/recommend                 获取最优分组推荐
```

### 模型相关
```
GET    /api/models/status                   获取所有模型状态
GET    /api/models/:model/status/history    获取模型历史数据
GET    /api/model/recommend                 获取最优模型推荐
```

### 告警相关
```
GET    /api/alerts                          获取用户告警列表（支持过滤和分页）
POST   /api/alerts/:id/mark_read            标记告警为已读
GET    /api/alerts/preferences              获取用户告警偏好
POST   /api/alerts/preferences              更新用户告警偏好
```

---

## 七、前端组件列表

### 新增组件

```
src/components/
├─ StatusBadge.tsx              状态徽章 (健康/警告/不健康/不可用)
├─ MetricsTrendChart.tsx        指标趋势图
└─ RecommendationPanel.tsx      推荐建议面板

src/features/dashboard/components/
├─ GroupStatusCard.tsx          分组状态卡片
├─ ModelStatusCard.tsx          模型排行卡片
└─ PerformanceComparison.tsx    性能对比表

src/features/alerts/
├─ AlertCenter.tsx              通知中心容器
├─ AlertItem.tsx                单个告警展示
├─ AlertPreferences.tsx         通知偏好设置
└─ index.tsx                    页面入口
```

### 修改的组件

```
src/features/keys/components/
├─ api-keys-columns.tsx         [修改] 添加分组状态列
└─ api-keys-table.tsx           [修改] 集成新列

src/features/pricing/components/
└─ model-card.tsx               [修改] 显示状态和替代方案

src/routes/_authenticated/dashboard/
└─ index.tsx                    [修改] 集成新卡片
```

---

## 八、环境变量配置 (可选)

```bash
# 状态聚合任务间隔（秒）
GROUP_STATUS_AGGREGATION_INTERVAL=3600

# 告警生成任务间隔（秒）
ALERT_CHECK_INTERVAL=300

# 告警保留天数
ALERT_RETENTION_DAYS=90

# 默认告警阈值
DEFAULT_SUCCESS_RATE_THRESHOLD=95
DEFAULT_RESPONSE_TIME_THRESHOLD=3000

# 推荐权重（%）
RECOMMEND_RELIABILITY_WEIGHT=40
RECOMMEND_SPEED_WEIGHT=30
RECOMMEND_COST_WEIGHT=30
```

---

## 九、Git提交建议

```bash
# 提交顺序
git commit -m "feat: 添加分组/模型状态数据模型"
git commit -m "feat: 实现状态计算和聚合service"
git commit -m "feat: 添加状态查询API端点"
git commit -m "feat: 集成定时聚合和告警任务"
git commit -m "feat: 前端仪表盘显示分组状态"
git commit -m "feat: 前端API Keys页面增强"
git commit -m "feat: 前端通知中心页面"
git commit -m "test: 添加单元测试和集成测试"
git commit -m "docs: 更新API文档和用户指南"
```

---

## 十、性能基准数据

目标指标（部署后应监控）：

| 指标 | 目标 | 说明 |
|------|------|------|
| API响应时间 | <500ms | GET /api/group/status |
| 告警生成延迟 | <10分钟 | 从问题发生到告警生成 |
| 数据库大小增长 | <1GB/年 | 3种history表，90天保留 |
| 定时任务CPU占用 | <5% | 三个定时任务的总和 |
| 用户采纳率 | >50% | 使用推荐功能的用户比例 |

---

## 十一、故障排查快速指南

### 问题：分组状态始终显示为"不可用"

**可能原因：**
1. consume_log中没有该分组的记录
2. 自动测试任务未启动
3. 数据库连接失败

**排查步骤：**
```sql
-- 检查consume_log是否有数据
SELECT COUNT(*), group FROM consume_log 
WHERE created_at > now() - INTERVAL 1 HOUR 
GROUP BY group;

-- 检查group_status表是否更新
SELECT * FROM group_status WHERE user_group = 'default';
```

### 问题：告警从未生成

**可能原因：**
1. 定时任务未启动（检查main.go中的StartAlertsGenerationTask())
2. 所有分组状态都正常（没有触发条件）
3. 数据库写入失败

**排查步骤：**
```bash
# 查看日志中是否有错误
grep "CheckGroupHealthAlerts\|error" app.log

# 手动触发一次聚合
# (需在controller中添加DEBUG接口)
```

### 问题：推荐总是相同的分组

**可能原因：**
1. 推荐算法逻辑有问题
2. 分组数据缺失或不准确

**排查步骤：**
```go
// 在service/group_status_service.go中添加日志
func (s *GroupStatusService) RecommendBetterGroups(currentGroup string) {
    log.Printf("Current group: %s", currentGroup)
    log.Printf("All groups: %v", allGroups)
    // ...
}
```

---

## 十二、推荐学习顺序

1. **先阅读**：功能方案文档（理解需求）
2. **后阅读**：实现架构文档（理解实现）
3. **再查看**：本文档（快速参考）
4. **然后**：按检查清单逐步实现
5. **最后**：参考代码框架文档编写具体代码

---

**最后更新**: 2026-06-02
**维护者**: AI Design Team
**反馈**: 如有问题，请参考详细文档或提出Issue

