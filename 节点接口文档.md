# 节点控制服务接口文档

## 1. 通用说明

### 1.1 服务地址

服务分为两个端口，地址来自 `.env`：

| 端口 | 用途 | 默认地址 | 配置项 |
|---|---|---|---|
| HTTP | 查询接口（节点列表、账号列表、删除节点） | `https://127.0.0.1:8888` | `LISTEN_ADDR` |
| WebSocket | 节点上报（心跳、账号状态） | `wss://127.0.0.1:8886` | `WS_LISTEN_ADDR` |

HTTP 接口路径统一前缀：

```text
/api/v1
```

WebSocket 连接路径：

```text
/ws
```

### 1.2 认证方式

两个端口均使用 mTLS 双向证书认证。

客户端连接时需要携带客户端证书和私钥，并使用 CA 证书校验服务端证书。

```text
certs/client.crt
certs/client.key
certs/ca.crt
```

### 1.3 数据格式

- HTTP 接口请求体和响应体均为 JSON，请求头需设置 `Content-Type: application/json`。
- WebSocket 消息格式为 JSON 文本帧，见第 2 节。

### 1.4 节点唯一标识

服务端使用 `node_name` 唯一标识一个节点：

- `node_name`：客户端在消息体或路径参数中提供，全局唯一。
- `public_ip`：服务端从 TCP 连接的 `RemoteAddr` 中提取，保存在节点信息中，不用于定位节点。

Redis 中节点字段名格式：

```text
{node_name}
```

### 1.5 节点状态

节点状态字段为 `status`：

| 值 | 说明 |
|---|---|
| `online` | 节点最近有心跳上报 |
| `offline` | 节点超过离线阈值未上报心跳 |

当前离线阈值为 60 秒。离线检查器每 5 秒扫描一次节点。

---

## 2. WebSocket 节点上报

### 2.1 连接说明

节点通过 WebSocket 长连接上报心跳和账号状态。连接地址：

```text
wss://<host>:8886/ws
```

连接建立后，节点持续发送 JSON 文本消息，服务端逐条回复。

### 2.2 消息格式

**节点 → 服务端（上行）：**

```json
{
  "type": "<消息类型>",
  "data": { ... }
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `type` | string | 消息类型，取值 `heartbeat` 或 `accounts` |
| `data` | object | 消息体，内容依 `type` 不同而不同 |

**服务端 → 节点（下行）：**

```json
{ "type": "ack", "status": "ok" }
```

或出错时：

```json
{ "type": "error", "message": "<错误描述>" }
```

### 2.3 心跳消息（type: heartbeat）

`data` 字段内容：

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `internal_ip` | string | 是 | 节点内网 IP |
| `node_name` | string | 是 | 节点名称 |
| `listen_port` | number | 是 | 节点监听端口，范围 1 到 65535 |
| `cpu_usage` | number | 否 | CPU 使用率 |
| `mem_usage` | number | 否 | 内存使用率 |
| `upload_bandwidth` | number | 否 | 当前系统上行带宽 |
| `download_bandwidth` | number | 否 | 当前系统下行带宽 |
| `today_requests` | number | 否 | 今日请求数 |
| `total_requests` | number | 否 | 总请求数 |
| `today_consumption` | number | 否 | 今日消费 |
| `total_consumption` | number | 否 | 总消费 |
| `total_rpm` | number | 否 | 累计 RPM，节点启动以来的每分钟平均请求数 |
| `current_rpm` | number | 否 | 当前 RPM，最近一分钟的每分钟请求数 |
| `avg_response_time` | number | 否 | 平均响应时间 |

**示例：**

```json
{
  "type": "heartbeat",
  "data": {
    "internal_ip": "10.0.0.12",
    "node_name": "node-001",
    "listen_port": 20001,
    "cpu_usage": 23.5,
    "mem_usage": 61.2,
    "upload_bandwidth": 128.5,
    "download_bandwidth": 512.3,
    "today_requests": 1200,
    "total_requests": 98000,
    "today_consumption": 32.45,
    "total_consumption": 2048.8,
    "total_rpm": 120.0,
    "current_rpm": 86.5,
    "avg_response_time": 315.2
  }
}
```

### 2.4 账号状态消息（type: accounts）

`data` 字段内容：

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `node_name` | string | 是 | 节点名称 |
| `accounts` | array | 是 | 账号状态列表，传空数组表示该节点当前没有账号 |

`accounts` 数组对象：

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | 是 | 账号唯一标识 |
| `name` | string | 否 | 账号显示名称 |
| `status` | string | 是 | 账号当前状态，`正常` 视为可用，其他视为异常 |
| `req` | number | 否 | 该账号的请求次数 |
| `tokens` | number | 否 | 该账号消耗的 token 数 |
| `account_cost` | number | 否 | 该账号产生的账号费用 |
| `user_cost` | number | 否 | 该账号产生的用户费用 |
| `total_capacity` | number | 否 | 该账号的总容量限额 |
| `used_capacity` | number | 否 | 该账号已消耗的容量 |

**示例：**

```json
{
  "type": "accounts",
  "data": {
    "node_name": "node-001",
    "accounts": [
      {
        "id": "account-001",
        "name": "账号001",
        "status": "正常",
        "req": 1200,
        "tokens": 48000,
        "account_cost": 0.96,
        "user_cost": 1.44,
        "total_capacity": 100.0,
        "used_capacity": 45.2
      },
      {
        "id": "account-002",
        "name": "账号002",
        "status": "封号",
        "req": 300,
        "tokens": 12000,
        "account_cost": 0.24,
        "user_cost": 0.36,
        "total_capacity": 50.0,
        "used_capacity": 12.8
      }
    ]
  }
}
```

清空账号列表：

```json
{
  "type": "accounts",
  "data": {
    "node_name": "node-001",
    "accounts": []
  }
}
```

### 2.5 错误说明

| 错误消息 | 场景 |
|---|---|
| `invalid message format` | 消息不是合法 JSON 或缺少 `type` 字段 |
| `unknown type: <type>` | `type` 不是 `heartbeat` 或 `accounts` |
| `internal_ip, node_name and listen_port(1-65535) are required` | 心跳必填字段缺失或端口不合法 |
| `node_name is required` | 账号消息缺少 `node_name` |
| `node not found, send heartbeat first` | 节点未注册，需先发送心跳 |
| `node is offline, send heartbeat first` | 节点处于离线状态，需先发送心跳 |
| `failed to save node info` | 服务端保存节点信息失败 |
| `failed to save account status` | 服务端保存账号状态失败 |

---

## 3. 获取节点列表

### 3.1 接口说明

消费端调用该接口获取当前保存的全部节点基础信息。

该接口只返回节点信息，不返回账号列表。账号列表需要调用"获取指定节点账号列表"接口。

返回结果按 `node_name` 升序排序。

### 3.2 请求信息

```http
GET /api/v1/nodes
```

### 3.3 请求参数

无请求体参数。

### 3.4 成功响应

状态码：`200`

```json
{
  "nodes": [
    {
      "public_ip": "127.0.0.1",
      "internal_ip": "10.0.0.12",
      "node_name": "node-001",
      "listen_port": 20001,
      "cpu_usage": 23.5,
      "mem_usage": 61.2,
      "upload_bandwidth": 128.5,
      "download_bandwidth": 512.3,
      "today_requests": 1200,
      "total_requests": 98000,
      "today_consumption": 32.45,
      "total_consumption": 2048.8,
      "total_rpm": 120.0,
      "current_rpm": 86.5,
      "avg_response_time": 315.2,
      "status": "online",
      "last_seen": "2026-05-27T17:00:00+08:00"
    }
  ]
}
```

节点对象字段：

| 字段名 | 类型 | 说明 |
|---|---|---|
| `public_ip` | string | 服务端从连接中识别出的节点公网 IP |
| `internal_ip` | string | 节点上报的内网 IP |
| `node_name` | string | 节点名称 |
| `listen_port` | number | 节点监听端口 |
| `cpu_usage` | number | CPU 使用率 |
| `mem_usage` | number | 内存使用率 |
| `upload_bandwidth` | number | 当前系统上行带宽 |
| `download_bandwidth` | number | 当前系统下行带宽 |
| `today_requests` | number | 今日请求数 |
| `total_requests` | number | 总请求数 |
| `today_consumption` | number | 今日消费 |
| `total_consumption` | number | 总消费 |
| `total_rpm` | number | 累计 RPM，节点启动以来的每分钟平均请求数 |
| `current_rpm` | number | 当前 RPM，最近一分钟的每分钟请求数 |
| `avg_response_time` | number | 平均响应时间 |
| `status` | string | 节点状态，`online` 或 `offline` |
| `last_seen` | string | 服务端最近一次收到该节点心跳的时间 |

### 3.5 错误响应

| 状态码 | 场景 | 响应示例 |
|---|---|---|
| `500` | 获取节点列表失败 | `{"error":"failed to list nodes"}` |

---

## 4. 获取指定节点账号列表

### 4.1 接口说明

消费端调用该接口获取指定节点最近一次上传的账号状态列表。

如果节点存在，但还没有上传过账号状态，接口返回空账号列表。

### 4.2 请求信息

```http
GET /api/v1/nodes/:node_name/accounts
```

### 4.3 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `node_name` | string | 是 | 节点名称 |

路径参数中如果包含特殊字符，需要进行 URL 编码。

### 4.4 请求示例

```http
GET /api/v1/nodes/node-001/accounts
```

### 4.5 成功响应

状态码：`200`

```json
{
  "public_ip": "127.0.0.1",
  "node_name": "node-001",
  "accounts": [
    {
      "id": "account-001",
      "name": "账号001",
      "status": "正常",
      "req": 1200,
      "tokens": 48000,
      "account_cost": 0.96,
      "user_cost": 1.44,
      "total_capacity": 100.0,
      "used_capacity": 45.2
    }
  ],
  "updated_at": "2026-05-27T17:01:00+08:00"
}
```

响应字段：

| 字段名 | 类型 | 说明 |
|---|---|---|
| `public_ip` | string | 节点公网 IP |
| `node_name` | string | 节点名称 |
| `accounts` | array | 节点最近一次上传的账号状态列表 |
| `updated_at` | string | 服务端最近一次收到该节点账号状态的时间；未上传过时为空时间 |

`accounts` 数组对象：

| 字段名 | 类型 | 说明 |
|---|---|---|
| `id` | string | 账号唯一标识 |
| `name` | string | 账号显示名称 |
| `status` | string | 账号当前状态，`正常` 视为可用，其他视为异常 |
| `req` | number | 该账号的请求次数 |
| `tokens` | number | 该账号消耗的 token 数 |
| `account_cost` | number | 该账号产生的账号费用 |
| `user_cost` | number | 该账号产生的用户费用 |
| `total_capacity` | number | 该账号的总容量限额 |
| `used_capacity` | number | 该账号已消耗的容量 |

### 4.6 错误响应

| 状态码 | 场景 | 响应示例 |
|---|---|---|
| `400` | 路径参数为空 | `{"error":"node_name is required"}` |
| `404` | 节点不存在 | `{"error":"node not found"}` |
| `500` | 账号状态数据损坏 | `{"error":"account status data invalid"}` |
| `500` | 查询账号状态失败 | `{"error":"failed to get account status"}` |

---

## 5. 删除离线节点

### 5.1 接口说明

删除指定节点，但只允许删除 `offline` 状态的节点。

删除成功时，会同时删除该节点的账号状态数据。

在线节点不能删除，会返回 `409`。

### 5.2 请求信息

```http
DELETE /api/v1/nodes/:node_name
```

### 5.3 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `node_name` | string | 是 | 节点名称 |

路径参数中如果包含特殊字符，需要进行 URL 编码。

### 5.4 请求示例

```http
DELETE /api/v1/nodes/node-001
```

### 5.5 成功响应

状态码：`200`

```json
{
  "status": "deleted"
}
```

### 5.6 错误响应

| 状态码 | 场景 | 响应示例 |
|---|---|---|
| `400` | 路径参数为空 | `{"error":"node_name is required"}` |
| `404` | 节点不存在 | `{"error":"node not found"}` |
| `409` | 节点不是离线状态，不能删除 | `{"error":"only offline nodes can be deleted"}` |
| `500` | 删除节点失败 | `{"error":"failed to delete node"}` |

---

## 6. 调用示例

### 6.1 Python 节点上报（WebSocket）

```python
import json
import ssl
import websocket

ssl_opts = {
    "certfile": "certs/client.crt",
    "keyfile":  "certs/client.key",
    "ca_certs": "certs/ca.crt",
}

ws = websocket.WebSocket(sslopt=ssl_opts)
ws.connect("wss://127.0.0.1:8886/ws", timeout=10)

# 发送心跳
ws.send(json.dumps({
    "type": "heartbeat",
    "data": {
        "internal_ip": "10.0.0.12",
        "node_name": "node-001",
        "listen_port": 20001,
        "cpu_usage": 23.5,
        "total_rpm": 120.0,
        "current_rpm": 86.5
    }
}))
print(ws.recv())  # {"type":"ack","status":"ok"}

# 发送账号状态
ws.send(json.dumps({
    "type": "accounts",
    "data": {
        "node_name": "node-001",
        "accounts": [
            {"id": "account-001", "name": "账号001", "status": "正常"}
        ]
    }
}))
print(ws.recv())  # {"type":"ack","status":"ok"}

ws.close()
```

### 6.2 curl 获取节点列表

```bash
curl --cert certs/client.crt \
  --key certs/client.key \
  --cacert certs/ca.crt \
  https://127.0.0.1:8888/api/v1/nodes
```

### 6.3 curl 获取节点账号列表

```bash
curl --cert certs/client.crt \
  --key certs/client.key \
  --cacert certs/ca.crt \
  https://127.0.0.1:8888/api/v1/nodes/node-001/accounts
```

### 6.4 curl 删除离线节点

```bash
curl --cert certs/client.crt \
  --key certs/client.key \
  --cacert certs/ca.crt \
  -X DELETE https://127.0.0.1:8888/api/v1/nodes/node-001
```

### 6.5 Python 模拟脚本

模拟节点上报（WebSocket）：

```bash
pip install websocket-client
python pytest/simulate_nodes.py --count 5 --accounts-per-node 3 --once
```

模拟消费端读取（HTTP）：

```bash
python pytest/simulate_consumer.py
```
